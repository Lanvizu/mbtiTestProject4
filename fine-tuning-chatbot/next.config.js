/** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig
module.exports = {
    future: {
        webpack5: true, // 빌드 캐시를 활성화하려면 webpack5를 true로 설정합니다.
    },
};